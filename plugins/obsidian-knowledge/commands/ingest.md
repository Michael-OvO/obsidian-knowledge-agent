---
description: Ingest raw material from Inbox/ into teaching-quality, navigable Obsidian notes (recall → pipeline → reflect)
argument-hint: [optional note about what to ingest, e.g. "the PDF in Inbox"]
---

Run a full self-evolving ingestion.

**1. Recall.** If this vault has `.agents/learned/`, read `conventions.md` and
`examples.md` and scan `.agents/learned/skills/` for a playbook matching the input.
Let them shape this run. (See `.agents/self-evolution.md` § Phase 0.)

**2. Ingest → compile → distribute.** Use the `obsidian-knowledge` skill: read its
references in order, then execute Phases 0–6. Prefer the vault's `.agents/*.md` if
present (they may be customized); otherwise use the skill's bundled `references/`.

**3. Reflect.** Finish with Phase 7 from `.agents/self-evolution.md`: append a
journal entry, and propose any durable convention / example / playbook updates for
review. Commit the collection per the approval policy; surface rule-change diffs
before committing them.

$ARGUMENTS
